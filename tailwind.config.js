/** @type {import('tailwindcss').Config} */
export default {
  content: ['./views/**/*.ejs'],
  theme: {
    extend: {
      colors:{
        'verde-dark': '#004d40', // Reemplaza con el color que necesitas
        'marron-dark': '#3e2723', // Reemplaza con el color que necesitas
        'naranja': '#ff5722', // Reemplaza con el color que necesitas
        'naranja-dark': '#e64a19', // Reemplaza con el color que necesitas
        'verde': '#388e3c', // Reemplaza con el color que necesitas
        'verde-dark': '#2c6e2f', // Reemplaza con el color que necesitas
      }
    },
  },
  plugins: [],
}

